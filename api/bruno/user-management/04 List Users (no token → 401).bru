meta {
  name: 04 List Users (no token - 401)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: none
}

assert {
  res.status: eq 401
  res.body.error.code: eq unauthorized
}
